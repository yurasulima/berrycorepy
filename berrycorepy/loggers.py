import logging

dispatcher = logging.getLogger("berrycore.dispatcher")
event = logging.getLogger("berrycore.event")
middlewares = logging.getLogger("berrycore.middlewares")
scene = logging.getLogger("berrycore.scene")